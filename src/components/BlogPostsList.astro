---
const { posts } = Astro.props;
---

{posts.map((post: any) => 
    <a href={post.url} class="block">
        <div class="p-3 flex justify-between items-center hover:bg-yellow-800/20 transition-colors ease-in-out duration-300">
            <div>
                <h3>{post.frontmatter.title}</h3>
                <p class="text-sm text-amber-500/60">{post.frontmatter.pubDate}</p>
            </div>
            <p class="text-md text-amber-500/60">➜</p>
        </div>
    </a>
    )}