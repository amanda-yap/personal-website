---
const { tags } = Astro.props;
---

<div class="flex gap-2 flex-wrap">
    {tags.map((tag) => 
    <span
        class="text-xs bg-stone-800/10 border-1 border-amber-500/30 px-2 py-1">
        {tag}
    </span>
    )}
</div>