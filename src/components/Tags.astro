---
const { tags } = Astro.props;
---

<div class="flex gap-2">
    {tags.map((tag) => 
    <span
        class="text-xs bg-stone-500/10 border-1 border-stone-100/10 px-2 py-1">
        {tag}
    </span>
    )}
</div>