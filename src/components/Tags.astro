---
const { tags } = Astro.props;
---

<div class="flex gap-2">
    {tags.map((tag) => 
    <a href={`/tags/${tag}`}
        class="text-sm bg-teal-950 rounded-lg px-2 py-1 cursor-pointer hover:scale-103 transition ease-in-out duration-200">
        {tag}
    </a>
    )}
</div>